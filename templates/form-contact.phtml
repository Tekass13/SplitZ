<section class="contact-intro">
    <h1>Nous contacter</h1>
    <p>Vous avez une question ou une demande ? N'hésitez pas à nous contacter en remplissant le formulaire ci-dessous.</p>

    <?php if (isset($_SESSION['message_erreur'])): ?>
        <div class="alert alert-danger">
            <?= $_SESSION['message_erreur'];
            unset($_SESSION['message_erreur']); ?>
        </div>
    <?php endif; ?>

    <?php if (isset($_SESSION['message_success'])): ?>
        <div class="alert alert-success">
            <?= $_SESSION['message_success'];
            unset($_SESSION['message_success']); ?>
        </div>
    <?php endif; ?>

    <form method="post" action="index.php?route=send-mail" class="contact-form">
        <div class="form-group">
            <label for="email">Email <span class="required">*</span></label>
            <input type="email" id="email" name="email" value="" required>
        </div>

        <div class="form-group">
            <label for="sujet">Sujet</label>
            <input type="text" id="sujet" name="sujet" value="">
        </div>

        <div class="form-group">
            <label for="message">Message <span class="required">*</span></label>
            <textarea id="message" name="message" rows="6" required></textarea>
        </div>

        <div class="form-group form-submit">
            <button type="submit" class="button">Envoyer</button>
        </div>
    </form>
</section>